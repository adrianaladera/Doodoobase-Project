- To activate venv, type ". /environ/bin/activate"
- To deactivate venv, just type "deactivate"

- Type in "export FLASK_APP=store.py" before typing in "flask run"
- In the shop directory, type "flask run" to run the flask application.
